// src/types/index.ts
// Re-exporta tudo dos schemas Zod — tipos derivados automaticamente
export type {
  Titulo,
  Cliente,
  TotalFinal,
  ParseResult,
  ValidationResult,
} from '../validators/schemas'
